<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starcloud.ops.business.app.dal.mysql.favorite.AppFavoriteMapper">

    <!-- 查询收藏应用列表 -->
    <select id="listFavorite" resultType="com.starcloud.ops.business.app.api.market.vo.response.AppMarketRespVO">
        SELECT b.uid, b.NAME, b.description, b.categories
        FROM llm_app_favorite a
                 LEFT JOIN llm_app_market b ON a.app_uid = b.uid
        WHERE a.USER = #{userId}
          AND a.deleted = 0
          AND b.audit = 1
          AND b.deleted = 0
        ORDER BY a.create_time DESC
    </select>
</mapper>