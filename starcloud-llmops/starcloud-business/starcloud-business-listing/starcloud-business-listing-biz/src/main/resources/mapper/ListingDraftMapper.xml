<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.starcloud.ops.business.listing.dal.mysql.ListingDraftMapper">


    <select id="count" resultType="java.lang.Long">
        SELECT count(*) from (SELECT MAX(id) as id from listing_draft GROUP BY uid  ) t
    </select>

    <select id="getLatestDrafts" resultType="com.starcloud.ops.business.listing.dal.dataobject.ListingDraftDO">
        SELECT t1.* from listing_draft t1
        INNER JOIN (SELECT MAX(id) as id from listing_draft GROUP BY uid  ) t2 on t1.id = t2.id
        ORDER BY t1.${field} ${type} limit #{start},#{end}
    </select>


</mapper>